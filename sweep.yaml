program: sweep.py
method: grid # random, grid, bayes, hyperband 
metric:
  name: loss
  goal: minimize
parameters:
  learning_rate:
    value: .0001 
          # min: 0.00001
          # max: 0.01
  batch_size:
    values: [16, 32, 64]
